name: mpost
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.7.2
dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.7
  markdown: ^7.0.0
  flutter_html: ^3.0.0-alpha.6
  provider: ^6.1.1
  cupertino_icons: ^1.0.8
  go_router: ^13.2.0  # 添加go_router依赖
  highlight: ^0.7.0
  webview_flutter_plus: ^0.4.15
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
  assets:
    - assets/
    - assets/web/
    - assets/web/static/css/
    - assets/web/static/js/
